var local_var

fn main()
	self.local_var = "apple"
	var x = "apple"
	var y = "pen"
	var z = x + y
	env.print(x, " + ", y, " = ", z)
	z += "apple"
	env.print(z)
	self.local_var += z
	env.print(local_var)
